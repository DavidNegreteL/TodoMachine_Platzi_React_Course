import styled from 'styled-components';

export const TodoCounter = styled.h2`
    font-weight: normal;
    letter-spacing: 0.05em;
    border-bottom: 2px solid rgba(255, 255, 255, 0.3);
    padding-bottom: inherit;
`;