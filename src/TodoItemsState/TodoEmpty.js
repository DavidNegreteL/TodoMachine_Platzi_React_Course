import React from "react";
import * as Styled from './TodoEmpty.styled';

export const TodoEmpty = () => {
    return (
        <Styled.EmptyText>
            Crea tu primer TODO
        </Styled.EmptyText>
    );
};