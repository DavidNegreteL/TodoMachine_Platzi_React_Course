import React from "react";
import * as Styled from './TodoLoading.styled';

export const TodoLoading = () => {
    return (
        <Styled.LoadingText>
            Estamos cargando...
        </Styled.LoadingText>
    );
};