import React from "react";
import * as Styled from './TodosError.styled';

export const TodosError = () => {
    return (
        <Styled.ErrorText>
            Crea tu primer TODO
        </Styled.ErrorText>
    );
};